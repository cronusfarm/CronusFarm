apiVersion: 1

datasources:
  - name: CronusFarm SQLite
    uid: cronusfarm-sqlite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: false
    editable: true
    jsonData:
      # Grafana 서버(또는 컨테이너)에서 직접 접근 가능한 SQLite 파일 경로
      path: /var/lib/cronusfarm/smartfarm.sqlite

