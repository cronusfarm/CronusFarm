# CronusFarm HTTP Basic Auth (선택)
# 활성화: bash ~/CronusFarm/scripts/pi-nginx-set-basic-auth.sh <사용자>
# 비활성화: sudo rm /etc/nginx/cronusfarm-auth.conf && sudo systemctl reload nginx
# (이 파일을 sites-available에 복사하지 않으면 include 한 줄만 주석 처리됨)
