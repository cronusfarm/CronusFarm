# Text 패널에서 iframe(Node-RED Dashboard UI 등) 렌더링 허용
# 적용: deploy-cronusfarm-pi.ps1 가 Pi에 복사 후 daemon-reload + grafana 재시작
[Service]
Environment=GF_PANELS_DISABLE_SANITIZE_HTML=true
Environment=GF_PANELS_ALLOW_EMBEDDING=true
